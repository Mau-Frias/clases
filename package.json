{
    "name": "clases-monorepo",
    "private": true,
    "author": "Mauricio Frías <mauricio.fr.or@gmail.com>",
    "license": "MIT",
    "scripts": {
        "test": "vitest run",
        "dev": "pnpm -r run dev",
        "// --- CORE SCRIPTS ---": "",
        "bump:core": "pnpm --filter clases exec npm version patch --no-git-tag-version",
        "bump:core:minor": "pnpm --filter clases exec npm version minor --no-git-tag-version",
        "bump:core:major": "pnpm --filter clases exec npm version major --no-git-tag-version",
        "publish:core": "pnpm --filter clases build && pnpm --filter clases publish --no-git-checks",
        "// --- TAILWIND SCRIPTS ---": "",
        "bump:tw": "pnpm --filter clases-tailwind exec npm version patch --no-git-tag-version",
        "bump:tw:minor": "pnpm --filter clases-tailwind exec npm version minor --no-git-tag-version",
        "bump:tw:major": "pnpm --filter clases-tailwind exec npm version major --no-git-tag-version",
        "publish:tw": "pnpm --filter clases-tailwind build && pnpm --filter clases-tailwind publish --no-git-checks",
        "// --- UTILS ---": "",
        "build": "pnpm -r run build",
        "publish:all": "pnpm build && pnpm -r --filter \"./packages/**\" publish --no-git-checks"
    },
    "devDependencies": {
        "tsup": "^8.5.1",
        "typescript": "^5.3.3",
        "vitest": "^1.2.1"
    },
    "version": "0.0.1"
}
